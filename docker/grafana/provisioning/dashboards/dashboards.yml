apiVersion: 1

providers:
  - name: 'PayStream'
    orgId: 1
    folder: 'PayStream'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
