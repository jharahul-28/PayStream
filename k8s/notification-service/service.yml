apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: paystream
  labels:
    app: notification-service
    app.kubernetes.io/part-of: paystream
spec:
  selector:
    app: notification-service
  ports:
    - name: http
      port: 8085
      targetPort: 8085
  type: ClusterIP
