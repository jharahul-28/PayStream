apiVersion: v1
kind: Service
metadata:
  name: settlement-service
  namespace: paystream
  labels:
    app: settlement-service
    app.kubernetes.io/part-of: paystream
spec:
  selector:
    app: settlement-service
  ports:
    - name: http
      port: 8086
      targetPort: 8086
  type: ClusterIP
