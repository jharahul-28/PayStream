apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: paystream
  labels:
    app: payment-service
    app.kubernetes.io/part-of: paystream
spec:
  selector:
    app: payment-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
  type: ClusterIP
