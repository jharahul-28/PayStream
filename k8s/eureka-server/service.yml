apiVersion: v1
kind: Service
metadata:
  name: eureka-server
  namespace: paystream
  labels:
    app: eureka-server
    app.kubernetes.io/part-of: paystream
spec:
  selector:
    app: eureka-server
  ports:
    - name: http
      port: 8761
      targetPort: 8761
  type: ClusterIP
