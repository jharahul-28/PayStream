apiVersion: v1
kind: Service
metadata:
  name: fraud-service
  namespace: paystream
  labels:
    app: fraud-service
    app.kubernetes.io/part-of: paystream
spec:
  selector:
    app: fraud-service
  ports:
    - name: http
      port: 8088
      targetPort: 8088
  type: ClusterIP
