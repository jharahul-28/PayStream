apiVersion: v1
kind: Service
metadata:
  name: ledger-service
  namespace: paystream
  labels:
    app: ledger-service
    app.kubernetes.io/part-of: paystream
spec:
  selector:
    app: ledger-service
  ports:
    - name: http
      port: 8084
      targetPort: 8084
  type: ClusterIP
